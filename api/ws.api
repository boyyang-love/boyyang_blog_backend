syntax = "v1"

type WsReq {
    Id uint `json:"id"`
}

type WsRes {
    Msg string `json:"msg"`
}

@server(
    jwt: Auth
    group: ws
)
service blog-api {
    @handler Ws
    get /ws (WsReq) returns (WsRes)
}
